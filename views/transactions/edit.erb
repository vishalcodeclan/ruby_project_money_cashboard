<h2>Edit <%= @transaction.id %></h2>

<form class="" action="/transactions/<%= @transaction.id %>" method="post">

  <label for="transaction_date_input">Select Date:</label>
  <input id="transaction_date_input" type="date"
  name="transaction_date" value="<%= @transaction.transaction_date %>"/>

  <label for="amount_input">Transaction Amount:</label>
  <input id="amount_input" type="number"
  name="amount" step="0.01" value="<%= @transaction.amount %>">

  <label for="category_select">Select Category:</label>
  <select class="" name="category_id" id="category_select" selected="<%= @transaction.category_id %>">
    <% @categories.each do |category| %>
    <% if category.id == @transaction.category_id %>
    <option value=<%= category.id %> selected><%= category.name.capitalize %></option>
    <% else %>
    <option value=<%= category.id %>><%= category.name.capitalize %></option>
          <% end %>
          <% end %>
  </select>

  <label for="vendor_select">Select Vendor:</label>
  <input required type="text" name="name" list="vendor_select" value="<%= @transaction.vendor.name %>">
  <datalist id="vendor_select">
    <% @vendors.each do |vendor| %>
    <option value=<%= vendor.name %>><%= vendor.name.capitalize %></option>
          <% end %>
  </datalist>


<input type="submit" name="" value="submit">




</form>
